import classNames from "classnames/bind";
import styles from "./PhoneForm.module.scss";

const cx = classNames.bind(styles);

function PhoneForm() {
  return <h2> Phone Form </h2>;
}

export default PhoneForm;
